<p style="color: green"><%= notice %></p>

<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <%= render user %>
    <p>
      <%= link_to "Show this user", user %>
    </p>
  <% end %>
<%= link_to "Add new stamp", new_stamp_path%>
</div>

<%= link_to "New user", new_user_path %>
